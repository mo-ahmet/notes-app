{
  "name": "notes-app",
  "version": "1.0.0",
  "description": "A simple note taking app following andrew mead's nodejs course",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --ext .js --ignore-path .gitignore ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.1",
    "joi": "^17.9.2",
    "yargs": "^12.0.2"
  }
}
